# Repair handoff — 7 September 2026

The implementation pass is saved, but the full 116-item research objective is
**not complete**. No accuracy improvement or publication readiness is established.
The complete item-by-item record is in change_tracker.json; detailed evidence and
remaining work are in implementation_status.md.

## Verified software repairs

Final regression suite: **321 tests passed**. Git diff whitespace check passed.

- All-record accuracy denominators, no-answer accounting and incomplete-run disclosure.
- Independent-vote and bounded-debate controls in the canonical runner.
- Actual generation accounting, read-only upstream reuse and cached-path finalization fix.
- Unusable witness prose and invalid generated state pairs withheld from judge input.
- Caption-preserving typed repair, rejected repair audits retained, evidence ancestry,
  duplicate-root suppression and explicit fallback/incomplete-explanation records.
- Failed execution/evaluation and interruption now persist honest progress states.
- Separate development/calibration manifests; existing dataset unchanged.
- Blinded image-preserving review packets with empty human ratings.
- Ten archived papers, 40 verified source-file hashes and explicit adaptation attribution.

## Real-model outcome

Full development qualification D completed three cases: baseline and tribunal both
2/3 correct, no accepted correction, all three judge contracts and caption witnesses
unusable. It made74generation requests. This is debugging evidence, not a performance
estimate. Review replay E tested wider text allowance and failed all three again,
with six actual judge calls, peak allocated memory7.0644GB and no OOM recovery.
The failure is therefore not demonstrated to be a GPU-capacity problem.

The independent-vote run saved three unchanged fallback answers and reused upstream
generation results, but failed during final timing export. The software error is
fixed and regression-tested; the original run remains marked failed_finalization.
Separate post-failure metrics do not relabel it successful.

## Research gates still open

Judge/witness semantic and output-contract qualification; calibrated acceptance
policy; stronger compute-matched controls; adequate held-out grouped comparisons;
and two actual independent human reviewers. The gated upstream reconstruction
remains deferred at the user's request. No stored split or label was changed.

Do not expand to a large GPU evaluation or claim all116items finished merely to
exhaust a usage allowance. Preserve negative results and continue from the failed
qualification evidence, not another unsupported increase in output limits.
