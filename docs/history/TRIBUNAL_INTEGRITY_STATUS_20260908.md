# Tribunal integrity implementation — 8 September 2026

## Current release status: critical semantic qualification remains open

Source/contract accounting, scoped verification, typed counterargument checks, final-decision application and bounded witness follow-up repairs are installed. The dataset, official splits, three figurative categories, model weights and feedback memory were not changed. Existing user changes were preserved and exact pre-change copies retained.

The original initial classifier remains active: the tested redesign regressed from 6/10 to 4/10 and was rolled back. The tribunal candidate completed ten frozen-checkpoint cases at 6/10 with zero accepted changes. A controlled alternative-verifier test using the existing Agent 1 runtime completed six binary-proposal cases with zero accepted changes. No replacement model is enabled, and useful correction reliability has not been established.

The final deterministic suite executed 406 tests: 402 passed, four documented expected failures, no unexpected failures/errors/skips. Two expected failures are unresolved initial-classifier requirements; two are older sentence-completion diagnostics. Do not describe them as repaired. The correction path is proven to apply admissible changes in regression fixtures, not yet reliably on the live qualification cases.

All ten installed-contract re-audits have internally consistent validity fields, intact source identity and **unqualified semantic interpretation/decomposition**. Source identity is not semantic accuracy. Binary answer validity is not directional-evidence validity.

Late changes add deterministic same-direction counterargument handling, removal of inactive flat claims/unanchored readings from judge prompts, accurate executed-comparison counts, and actual failed-obligation routing through the bounded second hearing. They passed integration tests and static saved-proof re-audit. A fresh complete live run of that final integrated source remains necessary; the earlier checkpoint experiment is not that run.

To enable the actual correction path, explicitly select:

```text
--judge-mode tribunal --semantic-bridge-mode corroborated --feedback-mode disabled --hardware-profile 8gb
```

The existing CLI defaults remain judge-disabled / bridge-shadow for compatibility. Shadow mode records proposals without applying bridge corrections. Use a new run directory and a saved sample manifest for paired regression comparison; do not call these repeatedly inspected cases a held-out benchmark.

Detailed report, exact backup copies, deployment manifest, tests, model-output diagnostics and installed re-audit are saved on this machine in:

```text
C:\Users\Sai Prateek\OneDrive\Desktop\FigDebate_sent\FigDebate_sent\research\tribunal_integrity_20260908
```

Start with `IMPLEMENTATION_STATUS.md`. Evidence files: `text_qualification.json`, `tribunal_qualification.json`, `alternative_verifier_qualification.json`, `unit_summary.json`, `unit_tests.log`, and `installed_audit.json`. These research experiments and the rejected classifier candidate are outside the imported production pipeline.

Remaining requirements: qualify stable initial classification; fix and behaviorally qualify entity/scope and figurative-relation verification; qualify Agent 2 readings; run full integrated paired tests and larger held-out/budget-matched research controls. Content-addressed reuse of unchanged model-verified obligations remains a deferred optimization. Keep feedback disabled until accepted explanations, not merely labels, are qualified.
